'use strict';

/**
 * group-achievement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::group-achievement.group-achievement');
