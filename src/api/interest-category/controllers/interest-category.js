'use strict';

/**
 * interest-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::interest-category.interest-category');
